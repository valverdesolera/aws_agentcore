# Copy this file to .env and fill in the values
# NEVER commit .env to git

# AWS
AWS_PROFILE="juanvalsol"
AWS_DEFAULT_REGION="us-east-1"
AWS_ACCOUNT_ID=""

# Langfuse Observability (US region)
LANGFUSE_SECRET_KEY="sk-lf-..."
LANGFUSE_PUBLIC_KEY="pk-lf-..."
LANGFUSE_BASE_URL="https://us.cloud.langfuse.com"

# AWS Cognito — filled in after terraform apply
COGNITO_USER_POOL_ID=""
COGNITO_CLIENT_ID=""
COGNITO_REGION="us-east-1"

# Secrets Manager — filled in after terraform apply (output: langfuse_secret_arn)
LANGFUSE_SECRET_ARN=""

# AgentCore endpoint — filled in after agentcore deploy
AGENTCORE_ENDPOINT=""
