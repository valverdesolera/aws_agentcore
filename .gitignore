# Virtual environment
teilurvenv/

# uv
.venv/
.python-version

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
dist/
build/
*.egg

# Environment variables (keep .env.example committed as a template)
.env
.env.local
.env.production

# OS
.DS_Store

# Terraform
infrastructure/.terraform/
infrastructure/*.tfstate
infrastructure/*.tfstate.backup
infrastructure/.terraform.lock.hcl
infrastructure/terraform.tfvars

# Downloaded PDFs (regenerated by ingest.py)
data/pdfs/

# FAISS vector index (regenerated by ingest.py; committed only after Phase 3)
# vectorstore/

# AgentCore build artifacts (zip bundles and dependency hashes — regenerated on deploy)
.bedrock_agentcore/**/*.zip
.bedrock_agentcore/**/*.hash
# Stale direct_code_deploy agent directory
.bedrock_agentcore/src_infrastructure_entrypoints_agentcore_handler/
